{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "python -c \"import os; print(f'PORT={os.environ.get(\"PORT\", 8000)}')\" > .env.port && uvicorn main:app --host 0.0.0.0 --port $(cat .env.port | cut -d= -f2)",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}